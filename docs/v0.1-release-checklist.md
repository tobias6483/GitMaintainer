# v0.1 Release Checklist

- [x] README documents current CLI behavior.
- [x] `python -m pytest` passes locally.
- [ ] CI passes on `main`.
- [x] Manual QA covers text, JSON, and badge output.
- [x] Manual QA covers package manifest detection for a repository with known manifests.
- [x] `CHANGELOG.md` has release notes.
- [x] Privacy and security docs still match implementation.
- [x] `python -m build` passes locally.
- [ ] CI package job passes.
- [x] v0.1.0 release decision is documented as GitHub pre-release first, PyPI deferred.

## Release Candidate Notes

GitMaintainer v0.1.0 should be published as a GitHub pre-release with attached
source and wheel artifacts. PyPI publication is intentionally deferred until
Trusted Publishing is configured.

Local release-readiness checks passed on 2026-06-04. CI items remain unchecked
until the release-readiness PR is merged and `main` checks complete.
